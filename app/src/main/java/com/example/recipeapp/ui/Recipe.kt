package com.example.recipeapp.ui

data class Recipe(
    val id: String,
    val name: String,
    val instructions: String,
    // Add more fields as needed (e.g., image URL, ingredients, etc.)
)